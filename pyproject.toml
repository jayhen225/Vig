[project]
name = "parlay"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb>=1.4.5",
    "nflreadpy>=0.1.5",
    "polars>=1.36.1",
    "pyarrow>=21.0.0",
]
