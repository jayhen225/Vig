# Python
__pycache__/
*.py[cod]
build/
dist/
wheels/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/

# Test / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Env / secrets
.env

# Rebuildable data (re-pullable from nflverse) -- not committed
data/raw/
data/*.duckdb

# NOTE: data/odds_raw/ is intentionally NOT ignored. Those odds snapshots are
# capture-or-lose (line movement can't be backfilled), so they must be committed.

# Editors / OS
.DS_Store
.idea/
.vscode/
